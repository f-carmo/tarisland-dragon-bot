import * as check from "./check";
import * as setchannel from "./set-channel-messaging"
import * as removechar from "./remove-character"
import * as list from "./list"
import * as resetlist from "./reset-list";

export const commands = {
  check, setchannel, removechar, list, resetlist
};